{
  "name": "sistema-cartorio-microservices",
  "version": "1.0.0",
  "description": "Sistema de cartório baseado em microsserviços",
  "main": "index.js",
  "scripts": {
    "dev": "docker-compose up -d",
    "build": "docker-compose build",
    "stop": "docker-compose down",
    "logs": "docker-compose logs -f",
    "install-all": "npm run install:services && npm run install:frontend",
    "install:services": "cd services && for dir in */; do (cd \"$dir\" && npm install); done",
    "install:frontend": "cd frontend && npm install",
    "push:quick": "powershell -ExecutionPolicy Bypass -File scripts/push-quick.ps1"
  },
  "keywords": [
    "cartorio",
    "microservices",
    "nodejs",
    "typescript",
    "react",
    "postgresql"
  ],
  "author": "Sistema Cartório",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "workspaces": [
    "services/*",
    "frontend",
    "shared"
  ]
}